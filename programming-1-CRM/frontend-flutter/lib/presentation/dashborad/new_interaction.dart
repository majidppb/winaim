import 'package:flutter/material.dart';

class NewInteractionScreen extends StatelessWidget {
  const NewInteractionScreen({super.key});

  static const path = 'new-interaction';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
